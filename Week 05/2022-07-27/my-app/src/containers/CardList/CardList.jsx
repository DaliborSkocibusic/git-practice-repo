import Card from "../../components/Card/Card";

const CardList = ({ titleFromApp }) => {
    return (
        // How to conditionally render something to our page
        <div></div>
    );
};

export default CardList;
