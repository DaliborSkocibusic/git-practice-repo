const input = { cat: "fish", dog: "beef", mouse: "cheese" };

let tempVar = input.fish;
let output = [];
output = Object.keys(input);

// output = output.forEach((element) => (element.toUpperCase()));


console.log(output);
